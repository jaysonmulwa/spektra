type twoDmatrix = number[][];    // 2D matrix

type lambdas = {lambdaOne: number, lambdaTwo: number} // Object with two outputs/params

type array2params = [number, number];

export { twoDmatrix, lambdas, array2params };   // Export types